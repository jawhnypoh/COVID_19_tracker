// United States Screen

import 'package:flutter/material.dart';

class USView extends StatelessWidget {
  const USView({Key key}) : super(key : key);

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
          child: Text('United States View'),
        )
    );
  }
}
