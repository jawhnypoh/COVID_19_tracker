// TimelineDateModel Class for individual date in historical data
class TimelineDateModel {
  String date;
  int confirmed;
  int deaths;
  int recovered;

  TimelineDateModel(this.date, this.confirmed, this.deaths, this.recovered,);
}